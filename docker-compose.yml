version: '3.7'
services:  
  nodejs:
    build: 
        context: .
        dockerfile: Dockerfile
    image: final-container 
    tty: true
    volumes:
      - $PWD/docs:/app/docs
      - $PWD/node_modules:/app/node_modules
    ports:
      - "8080:8080"
    working_dir: /app
